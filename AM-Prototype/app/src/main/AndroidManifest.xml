<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.a3sir.protechsolutions.am_prototype">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".CadastroActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".EsqueciActivity" />
        <activity android:name=".InvestirActivity" />
        <activity android:name=".InvestimentosActivity" />
        <activity android:name=".ResgatarInvestimentosActivity" />
        <activity android:name=".ChatbotActivity" />
        <activity android:name=".SaldoActivity"></activity>
    </application>

</manifest>